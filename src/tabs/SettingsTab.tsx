'use client';

import React from 'react';

export default function SettingsTab() {
  return (
    <div className="p-4">
      <h2 className="text-lg font-bold mb-2">設定用（未実装）</h2>
      <p className="text-sm text-gray-600">種目の追加・削除・名前変更・順序変更などをここで設定できるようにする予定です。</p>
    </div>
  );
}
